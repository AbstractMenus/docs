{
  "id": "04-lists-vs-objects",
  "title": "Lists vs objects",
  "topic": "basics",
  "intro": "HOCON has two structural shapes:\n\n- **Object** `{ key = value, ... }` - unordered collection of named fields. Used for menus, items, click actions, rules - anything with a known schema.\n- **Array** `[ value, value, ... ]` - ordered collection. Used for `items` (multiple slots in a menu), `lore` (a list of text lines), `actions` (a sequence to run on click).\n\nKey difference: objects have **names** for each field, arrays have **positions**. Use the right one or the parser will reject your config.\n\n**Goal:** add `tags` as a list of three strings AND `meta` as an object with `version = 1`.",
  "starter": "title = \"Demo\"\n",
  "goal": "Add `tags = [...]` (list of 3 strings) and `meta = { version = 1 }` (object).",
  "check": {
    "type": "shape",
    "asserts": [
      { "kind": "has", "path": "tags[2]" },
      { "kind": "eq", "path": "meta.version", "value": 1 }
    ]
  },
  "hints": [
    "Arrays go in `[...]`: `tags = [\"a\", \"b\", \"c\"]`. Items separated by comma OR newline.",
    "Objects go in `{...}`: `meta = { version = 1 }`. Same separator rules.",
    "```\ntags = [\"shop\", \"basic\", \"v1\"]\nmeta = { version = 1 }\n```"
  ],
  "next": null,
  "translations": {
    "ru": {
      "title": "Списки и объекты",
      "intro": "У HOCON две структурные формы:\n\n- **Объект** `{ key = value, ... }` - неупорядоченный набор именованных полей. Используется для меню, items, click-действий, rules - всего где есть схема.\n- **Массив** `[ value, value, ... ]` - упорядоченный набор. Используется для `items` (несколько slot'ов в меню), `lore` (список строк текста), `actions` (последовательность действий на клик).\n\nКлючевая разница: у объектов поля **именованные**, у массивов - **по позиции**. Используй правильную форму - иначе парсер ругнётся.\n\n**Цель:** добавь `tags` как список из трёх строк И `meta` как объект с `version = 1`.",
      "goal": "Добавь `tags = [...]` (список из 3 строк) и `meta = { version = 1 }` (объект).",
      "hints": [
        "Массивы оборачиваются в `[...]`: `tags = [\"a\", \"b\", \"c\"]`. Элементы разделяются запятой ИЛИ переносом строки.",
        "Объекты оборачиваются в `{...}`: `meta = { version = 1 }`. Те же правила разделителей.",
        "```\ntags = [\"shop\", \"basic\", \"v1\"]\nmeta = { version = 1 }\n```"
      ]
    }
  }
}
